name: Mi10Pro-cmi HyperOS1.0.5 Kernel Build (省电优化版)
on:
  workflow_dispatch:
    inputs:
      phone:
        description: 设备型号(固定cmi-小米10Pro)
        required: true
        default: 'MI10_Pro-cmi'
        type: choice
        options:
          - MI10_Pro-cmi

jobs:
  build_cmi_kernel:
    runs-on: ubuntu-22.04
    env:
      TARGET_DEVICE: cmi
      KERNEL_DIR: ${{ github.workspace }}/kernel_xiaomi_sm8250_mod
      CLANG_VERSION: 20210522
      ARCH: arm64
      SUBARCH: arm64
      HYPEROS_VER: 1.0.5
      GIT_COMMIT_ID: ${{ github.sha }}

    steps:
      - name: ⚙️ 安装系统依赖
        run: |
          sudo apt update -y
          sudo apt install -y build-essential git curl wget bison flex zip bc cpio libssl-dev \
            python3 python3-pip python-is-python3 libelf-dev dwarves rsync lz4 gcc-aarch64-linux-gnu \
            gcc-arm-linux-gnueabi binutils-aarch64-linux-gnu ccache
          sudo apt autoremove -y && sudo apt clean

      - name: 📥 拉取内核源码
        run: |
          git clone https://github.com/liyafe1997/kernel_xiaomi_sm8250_mod.git "$KERNEL_DIR"
          cd "$KERNEL_DIR"
          git checkout android15-lineage22-mod
          git fetch --depth 1 && git pull

      - name: 🛠 下载 Proton-Clang 20210522
        run: |
          mkdir -p "$HOME"/proton-clang
          cd "$HOME"/proton-clang
          wget -q --timeout=30 --tries=3 https://github.com/kdrag0n/proton-clang/archive/refs/tags/${CLANG_VERSION}.zip
          unzip -q ${CLANG_VERSION}.zip
          TOOLCHAIN_PATH="$HOME"/proton-clang/proton-clang-${CLANG_VERSION}/bin
          echo "TOOLCHAIN_PATH=$TOOLCHAIN_PATH" >> "$GITHUB_ENV"
          echo "$TOOLCHAIN_PATH" >> "$GITHUB_PATH"

      - name: 🚀 编译内核（深度省电优化 · 不卡不发热）
        run: |
          cd "$KERNEL_DIR"
          export PATH="$TOOLCHAIN_PATH:$PATH"

          MAKE_ARGS="ARCH=arm64 SUBARCH=arm64 O=out CC=clang \
          CROSS_COMPILE=aarch64-linux-gnu- \
          CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
          CLANG_TRIPLE=aarch64-linux-gnu- \
          LLVM=1 LLVM_IAS=1"

          # 轻量优化，不激进、不炸编译
          export KCFLAGS="-fno-strict-aliasing -fno-common -pipe -Wno-error -O2"
          export LDFLAGS=""

          # 版本号
          lv="-perf"
          lv_new="-HyperOS${HYPEROS_VER}-A13-$(date +%Y%m%d)-${GIT_COMMIT_ID:0:8}-省电版"
          sed -i "s|$lv|$lv_new|g" arch/arm64/configs/cmi_defconfig

          # 备份 DTS
          dts_source=arch/arm64/boot/dts/vendor/qcom
          cp -a "$dts_source" .dts.bak

          # 生成 config
          make $MAKE_ARGS cmi_defconfig

          # ==================== 省电核心优化 ====================
          scripts/config --file out/.config \
          # 基础
          -e KALLSYMS -e KALLSYMS_ALL \
          # 文件系统省电
          -e EROFS_FS -e F2FS_FS -e F2FS_FS_TRIM -e F2FS_IDLE_FLUSH \
          # zram 省电（减少后台交换）
          -e ZRAM -e ZRAM_WRITEBACK -e CRYPTO_LZ4 -e CRYPTO_ZSTD \
          # 最强省电：深度休眠、减少唤醒
          -e CPU_IDLE -e CPU_IDLE_LPM -e CPU_IDLE_GOV_LPMUNIVERSAL \
          -e SCHED_WALT -e SCHED_SMARTPRIORITY -e SCHED_POWER_EFFICIENT \
          # 降低后台耗电，延长待机
          -e ANDROID_SIMPLE_LMK -e LRU_GEN -e LRU_GEN_ENABLED \
          -e MI_RECLAIM -e BOOTUP_RECLAIM -e MEMCG_OOM_CONTROL \
          # 网络省电
          -e TCP_CONG_BBR -e DEFAULT_BBR -e NET_SCH_FQ_CODEL \
          # IO 省电（UFS3.0 低功耗）
          -e MQ_IOSCHED_KYBER -e IOSCHED_BFQ -e BFQ_POWER_EFFICIENT \
          # 小米/高通省电特性
          -e XIAOMI_MIUI -e MIHW -e MIGT -e MIGT_ENERGY_MODEL \
          -e PERF_HUMANTASK -e PERF_AUDIO_LOW_POWER \
          # 关闭调试、减少功耗开销
          -d DEBUG_FS -d DEBUG_INFO -d LTO_CLANG -d LOCALVERSION_AUTO \
          -d KSU -d KSU_SUSFS -d KPM \
          # GPU/总线省电
          -e DEVFREQ_GOV_SIMPLE_ONDEMAND -e DEVFREQ_GOV_POWERSAVE \
          -e CPU_FREQ_GOV_SCHEDUTIL -e CPU_FREQ_DEFAULT_GOV_SCHEDUTIL \
          -e CPU_FREQ_GOV_POWERSAVE -e CPU_ISOLATION

          # 编译线程降低，更稳
          make $MAKE_ARGS -j4 2>&1 | tee build.log

          if [ ! -f out/arch/arm64/boot/Image ]; then
            echo "❌ 编译失败"
            exit 1
          fi

          # 合并DTB
          find out/arch/arm64/boot/dts -name '*.dtb' -exec cat {} + > out/arch/arm64/boot/dtb

          # 恢复源码
          rm -rf "$dts_source"
          mv .dts.bak "$dts_source"
          sed -i "s|$lv_new|$lv|g" arch/arm64/configs/cmi_defconfig

          # 打包
          git clone https://github.com/liyafe1997/AnyKernel3 -b kona --depth 1 anykernel
          mkdir -p anykernel/kernels
          cp out/arch/arm64/boot/Image anykernel/kernels/
          cp out/arch/arm64/boot/dtb anykernel/kernels/

          cd anykernel
          ZIP=Mi10Pro-cmi_HyperOS1.0.5_$(date +%Y%m%d_%H%M%S)_省电优化版.zip
          zip -r9 "$ZIP" . -x '.git*'
          mv "$ZIP" "$KERNEL_DIR"/

      - name: 📦 查看产物
        run: |
          cd "$KERNEL_DIR"
          ls -lh *.zip out/arch/arm64/boot/Image

      - name: 📤 上传刷机包
        uses: actions/upload-artifact@v4
        with:
          name: Mi10Pro-cmi-Kernel-省电优化版
          path: |
            ${{ env.KERNEL_DIR }}/*.zip
            ${{ env.KERNEL_DIR }}/out/arch/arm64/boot/Image
            ${{ env.KERNEL_DIR }}/out/arch/arm64/boot/dtb
            ${{ env.KERNEL_DIR }}/build.log
          retention-days: 10
